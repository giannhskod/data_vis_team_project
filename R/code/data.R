data.flights.outbout = sqlite_to_df(db = "data/db.sqlite", query = query.flights.outbound)
data.delays.security.by_destination = sqlite_to_df(db = "data/db.sqlite", query = query.delays.security.by_destination)
